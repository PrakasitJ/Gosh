# Gosh Language Extension

Simple syntax highlighting extension for Gosh language.

## Installation

The extension provides syntax highlighting for `.gosh` files. The file association in `.vscode/settings.json` maps `.gosh` files to use Go syntax highlighting as a fallback.
